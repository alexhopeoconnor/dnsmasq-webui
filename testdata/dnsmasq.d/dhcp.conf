# Sample dhcp-host entries for integration testing (managed by this app).
# Use testnet IP (172.28.0.x) so static host is on same subnet as dhcp-range; outside range 172.28.0.10–50.
dhcp-host=aa:bb:cc:dd:ee:ff,172.28.0.100,testpc,infinite
