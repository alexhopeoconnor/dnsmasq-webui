# Extra upstream/local (included by conf-dir). Not managed by app; values here show as readonly in UI.
# Adds more server= and local= so ServerLocalValues has entries from multiple files.
server=/internal.lan/192.168.1.1
local=/localdomain/
