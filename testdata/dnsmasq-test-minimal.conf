# Minimal dnsmasq config for the test harness (use with --minimal-conf).
# Single file, no conf-dir; app adds conf-file= for managed config on startup.
# Use for testing reload/restart failure flows (fewer readonly options to edit).
port=53

log-facility=/data/dnsmasq.log
log-queries
