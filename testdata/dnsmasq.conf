# Sample main dnsmasq config (not managed by this app).
# In production this would typically be /etc/dnsmasq.conf.
# It includes all .conf files from dnsmasq.d/ (e.g. dhcp.conf managed by this app).
conf-dir=/etc/dnsmasq.d/

conf-file=zz-dnsmasq-webui.conf
