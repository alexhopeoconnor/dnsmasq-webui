namespace DnsmasqWebUI.Models.Filters;

/// <summary>High-level provenance for filter policy rows (for grouping in source dropdowns).</summary>
public enum FilterPolicySourceKind
{
    Managed,
    Included,
    Unknown
}
